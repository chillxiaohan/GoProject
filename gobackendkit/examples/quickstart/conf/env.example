# 数据库（PostgreSQL 示例；也可用 ERP_DB_* 同名变量）
# 注意：表会建在下面这个 DB_NAME 里；客户端看错库就会「看不到表」
DB_TYPE=postgresql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=kit_demo

# Redis
REDIS_ADDR=127.0.0.1:6379
REDIS_PASSWORD=
REDIS_DB=0

# 本示例 HTTP 监听
HTTP_ADDR=:8090
